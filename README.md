# API
Minimal REST API for managing notes, built with FastAPI.  Includes CRUD operations, request validation, API versioning, and a health check endpoint. Designed as a foundation for a production-ready backend service.
